import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import Example from '../detail/detail_second';

const HomeScreen = () => {
  return (
    <View flex={1}>
      <Example/>
    </View>
  );
};

export default HomeScreen;

const styles = StyleSheet.create({});
